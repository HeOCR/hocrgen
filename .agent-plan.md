# Agent Plan

This file tracks the current merged-main state of the repository.
Use merged code on `main` and merged GitHub PR history as the source of truth when reconciling roadmap status.
Do not treat branch-local execution notes as authoritative if they disagree with merged `main`.

## Current System State
- Last completed roadmap action on the current ref: `F6f2` hocrgen-owned import metadata sidecar (plus retroactive `F4f` upstream-chain documentation also on this ref)
- Current blockers:
  - no known blockers on the completed `E4` governance milestone
  - no known blockers on packaged benchmark approval lookup for non-editable installs
  - no known blockers on source-health operator path portability
  - no known blockers on defining the beta-scale acquisition trial plan
  - no known blockers on using the repo-native Splendor workspace for future agent context
  - no known blockers on the F4a four-repository synthetic spinout architecture or the F4f wider HeOCR ecosystem chain that extends it upstream through `public-domain-hand-written-hebrew-scans`, `hletterscriptgen`, and `hletterscript`
  - `F1d` hardening now surfaces deterministic near-duplicate, source-group, split-leakage, and benchmark/holdout leakage risks before scale beyond the operator-only F1c trial
  - F1c target-scale execution still has enforced public-beta blockers: synthetic target scale requires a larger validated hocrsyngen batch, and the current F1c artifacts are operator-only evidence rather than public beta readiness
  - `F1e` resolves the previously visible F1d benchmark/holdout source-group risk by requiring typed, repo-tracked accepted resolutions before exact duplicate, near-duplicate, or source-group overlap can be treated as resolved
  - no known blockers on the completed `F3` modern handwritten acquisition program through `F3b`; modern handwriting remains operator-only and requires explicit review approval before release inclusion
  - `F5a` now defines public beta publishability separately from operator trial success, with gates for source depth, composition, rights/provenance, privacy/review, duplicate/leakage control, benchmark references, portability, public docs, and takedown readiness
  - `F5b` now implements deliberate public beta packaging and handoff artifacts without repository sync, upload, release tagging, or publication-report emission while any readiness gate is blocked
  - `F5c` now derives a public beta blocker-closure plan from the readiness report, separates repo-owned blockers from external/input-dependent blockers, makes takedown private-reporting setup machine-readable, and keeps the synthetic target-scale gate blocked until a larger validated hocrsyngen batch exists
  - `F5d` now writes `manifests/public_beta_repo_owned_blocker_report.json`, recording item/status-level privacy-review, benchmark-reference, and takedown/private-reporting closure evidence while preserving external/input-dependent blockers for source depth and the hard `2 / 80` synthetic target-scale gap
  - `F6a` now defines the post-F5 public beta closure roadmap without changing runtime behavior, relaxing readiness gates, or claiming public beta publishability
  - `F6b` now records GitHub private vulnerability reporting for `HeOCR/hocrgen` as enabled and verified by an authenticated GitHub API repository-settings check, closing only the takedown/removal readiness gate while keeping benchmark-reference, privacy/review, source-depth/composition, and the hard `2 / 80` synthetic target-scale blockers blocked
  - `F6c` now evaluates current benchmark-reference evidence for public beta readiness, finds only partial reviewed/adjudicated coverage (`1 / 3` selected benchmark items ready), keeps `benchmark_references` blocked, and makes the limitation precise in `public_beta_readiness_report.json` and `public_beta_repo_owned_blocker_report.json`
  - `F6d` now evaluates current privacy/review evidence for public beta readiness, finds unresolved review-required item evidence with no repo-tracked review/config/source-status closure, keeps `privacy_review` blocked, and records the F6d limitation plus source-status evidence in `public_beta_repo_owned_blocker_report.json`
  - `F6e` now evaluates current source-depth/composition evidence for public beta readiness, finds only `2 / 80` real public-profile payload candidates against the planned `27` NLI / `27` Pinkas / `26` BiblIA source mix, keeps `source_depth_composition` blocked, and records the F6e limitation in `public_beta_source_depth_composition_report.json` plus `public_beta_repo_owned_blocker_report.json`
  - `F6f1` now records the hocrsyngen S6 handoff review and splits the synthetic target-scale path into an installed-CLI adapter preflight/diagnostic dry-run before later larger-batch integration; raw hocrsyngen public `generation_manifest.v1` output remains candidate input and is not wired into the current hocrgen release path
  - `F6f2a` now implements the operator-only `hocrgen hocrsyngen-preflight` evidence-root reader over hocrsyngen candidate evidence runs: it validates public reports/manifests/assets/catalog joins and emits a hocrgen diagnostic report while keeping `release_eligible: false`
  - `F6f2` now defines the hocrgen-owned `hocrgen_hocrsyngen_import_metadata_packet.v1` sidecar/import packet: preflight derives provider identity/version, per-sample rendering metadata, and per-sample Hebrew coverage from validated evidence roots, public manifests, template catalogs, and reports; validates the packet with a typed hocrgen-owned schema; records release/import projection as false unless the evidence root explicitly proves provider runtime flags; and keeps both the packet and the 80-sample evidence root operator-only with `release_eligible: false`
  - the remaining F6 follow-up path is final readiness convergence (`F6g`), and `F6g` still requires real privacy/review closure, public-profile source-depth evidence, and synthetic-scale release-path admission before it can pass
- Short-term release-scale path:
  - use an operator-only beta-scale acquisition trial before public beta/release export work
  - target `80` real items plus `80` synthetic controls, with real-source allocation fixed at `27` NLI, `27` Pinkas, and `26` BiblIA
  - use the live-but-cached NLI seeded acquisition operator path so vetted seed URLs can be materialized in batches without hand-promoting each item
  - require Pinkas/BiblIA source-depth expansion to stay packaged, fixture-backed, rights-safe, and reviewable before either source is treated as scalable beyond its bounded exemplars
  - keep committed/cached fixtures as the deterministic build input after acquisition; do not add network-dependent CI tests
  - keep rights, privacy, review, dedupe, split, benchmark, synthetic-cap, and export-portability gates in charge before any public beta or release-candidate export
  - treat operator trial success as necessary but not sufficient for public publishability; public beta additionally requires the F5a dataset-card, provenance, benchmark-reference, archive/checksum, portability, changelog, and takedown gates
  - treat `F1` as a gate-driven beta-readiness program, not a count-only acquisition run
  - keep deterministic near-duplicate/source-group leakage hardening in force before scale beyond the operator-only trial
  - keep benchmark/holdout leakage resolutions explicit and typed; unresolved or stale overlap must block F1 trial/report readiness rather than remain a passive warning
  - keep modern handwritten acquisition implementation, external synthetic-provider gates, and publication readiness as explicit follow-on milestones
  - treat synthetic-only dataset publication to `HeOCRsynth` as a separate release stream from mixed real+synthetic `HeOCR` publication
- Post-alpha follow-ups:
  - `F4a`: synthetic spinout planning defines `hocrsyngen` as the synthetic Hebrew OCR/HTR generation package, keeps `hocrgen` as the orchestration/governance/export pipeline, keeps `HeOCR` as the mixed real+synthetic dataset repository, and introduces `HeOCRsynth` as the synthetic-only dataset repository
  - `F4a`: generated `hocrsyngen` outputs are candidate synthetic inputs, not release-ready data by themselves; `hocrgen` remains responsible for rights, privacy, review, dedupe, split, benchmark, synthetic-cap, and export-portability gates
  - `F1a`: beta-scale trial planning defines an operator-only `80` real / `80` synthetic target, an even real-source mix of `27` NLI / `27` Pinkas / `26` BiblIA, explicit non-goals, source-depth feasibility gates, a GitHub issue template for follow-on implementation work, and a repo-native Splendor workspace for future coding-agent context
  - Splendor workspace: `wiki/`, `state/`, `planning/`, `reports/`, and `splendor.yaml` now provide generated source summaries, topic scaffolds, source manifests, run/queue state, and agent-brief/query entrypoints over 52 ingested high-signal repo sources
  - `F1b`: discover now emits an operator-only `source_depth_feasibility.json` report for the `80` real / `80` synthetic F1 target, including per-source target counts, health-eligible runnable/cached candidates, observed candidates, assets, exploratory catalog candidates, gaps, feasibility status, and operator notes
  - `F1b2`: Pinkas and BiblIA now have packaged source-depth expansion manifests wired into source health and the F1 feasibility report, so their expansion path is defined, fixture-backed, rights-safe, and reviewable before added records count
  - `F1b3`: source depth materially reduced the F1 inventory gaps by packaging `27` Pinkas records/assets and `26` BiblIA records/assets; after F4b, synthetic target scale requires a larger validated hocrsyngen manifest batch instead of hocrgen-side generator expansion
  - `F1b4`: NLI now has `27 / 27` real runnable/cached fixture-backed seeds after promoting the remaining exploratory catalog entries through the operator CDP promotion path; the newly promoted fixtures are source-depth-only and generated or hand-authored stand-ins did not count toward the gate
  - `F1c`: the explicit `hocrgen f1-beta-trial --profile profile_open_v1 --dry-run` operator path now exercises `27` NLI, `27` Pinkas, `26` BiblIA, and the currently configured hocrsyngen manifest samples through the existing gates, emits `build_release/f1_target_scale_trial_report.json`, and stays blocked for full synthetic target scale until a larger validated hocrsyngen batch is configured
  - `F1d`: near-duplicate, source-group, and split-leakage hardening now emits deterministic risk artifacts, groups related records for split safety, surfaces benchmark/holdout leakage risk, and keeps near-duplicates as manual-review risks rather than silently removing them
  - `F1e`: benchmark/holdout leakage now has an explicit typed resolution policy in `benchmark_v1`; exact duplicate, near-duplicate, and source-group overlap with non-benchmark holdout/public-beta candidates is blocked unless a repo-tracked accepted resolution matches the detected group and member set. The current Pinkas WDL11806 source group is deliberately resolved by excluding related non-benchmark pages from holdout/public-beta readiness claims while keeping `benchmark_v1` membership stable.
  - `F2`: benchmark ground-truth foundation now includes documented reference contracts plus optional runtime ingestion, adjudication/status artifacts, and versioning gates
  - `F2a`: benchmark ground-truth guidelines now define Hebrew transcription policy, layout-label policy, minimum `benchmark_transcription_reference.v1` and `benchmark_layout_reference.v1` child-reference shapes, and the documented `benchmark_reference_manifest.v1` contract while keeping public/alpha transcriptions optional
  - `F2b`: benchmark-reference ingestion now validates packaged/config-root reference manifests, child transcription/layout shapes, release-relative path portability, benchmark item/source/split linkage, layout asset path/checksum/dimension linkage, adjudication/status counts, child reference materialization for build/export artifacts, and correction/supersession versioning coherence over stable reference ids without making references mandatory for current public/alpha exports
  - `F3a`: modern handwritten acquisition policy now defines the rights-clean contemporary handwriting collection boundary for contributor consent, release terms, rights provenance, contemporary privacy screening, takedown/removal handling, scanning/upload standards, operator review, composition targets, and the separation between modern real handwriting, historical public sources, and synthetic data
  - `F3b`: hocrgen now validates operator-provided modern handwriting intake manifests, enforces adult contributor consent/provenance/privacy/takedown/scan metadata requirements, maps valid source-relative JPEG/PNG assets through a review-only source adapter, reports source health for configured modern intake sources, and requires explicit review decisions before public release inclusion
  - `F4b`: hocrgen now consumes fixture-backed hocrsyngen `generation_manifest.v1` batches through a manifest/file adapter, validates unique samples/pages/assets and relative JPEG page assets on the hocrgen side, preserves stable `project_synthetic:synthetic-*` item ids through an explicit compatibility mapping, and keeps governance/release/export gates unchanged
  - `F4c`: hocrgen now hardens the hocrgen-side hocrsyngen fixture/import boundary with required provider metadata, explicit no-network/no-REST/no-GPU/no-LLM/no-diffusion flags, per-sample Hebrew rendering metadata, computed Hebrew coverage validation, source-health signals, config validation, and synthetic composition reporting without importing or calling hocrsyngen internals
  - `F4d`: hocrgen now exports synthetic-only HeOCRsynth handoff trees with `hocrgen export-synthetic`, selecting only release-ready `PROJECT-SYNTHETIC` items from existing pipeline state, preserving synthetic disclosure and the hocrgen-side provider/rendering/Hebrew coverage metadata attached to hocrsyngen-backed items, writing payload assets under `data/synthetic/<split>/<item_id>/`, and keeping mixed `HeOCR` `export-alpha` releases distinct
  - `F4e`: hocrgen now keeps shared release export packaging primitives in `hocrgen.package.common`, so mixed `HeOCR` alpha exports and synthetic-only HeOCRsynth exports share release-relative portable payload shaping, stats, audit/review payloads, benchmark/reference filtering, release diffs, changelog rendering, and common manifest/doc writing while retaining separate release policy
  - `F4f`: the wider HeOCR ecosystem chain is now documented in `docs/heocr_ecosystem_overview.md`, the README, the roadmap, the synthetic asset contribution guide, and the design/spec: `public-domain-hand-written-hebrew-scans` is the upstream real-handwriting page-scan corpus, `hletterscriptgen` is the code/framework that crops those scans into per-letter glyph variants (`letter_set.v1`), `hletterscript` is the per-writer glyph-image dataset, and the existing `hocrsyngen → hocrgen → HeOCR / HeOCRsynth` chain consumes that synthetic input chain unchanged; `hocrgen` continues to import zero upstream code, call no upstream CLIs from default release/export paths, and re-validate no upstream rights or checksums; `F4f` is documentation-only
  - `F5a`: public beta readiness is now defined as a publishability contract, not a packaging implementation: it requires source-depth/composition evidence, validated synthetic target scale and cap policy, rights/provenance evidence, privacy/review closure, duplicate/source-group/split/benchmark leakage clearance, benchmark-reference readiness, portable manifests/checksums/archives, public dataset-card/provenance/changelog docs, and a takedown/removal workflow before mixed `HeOCR` public beta publication
  - `F5b`: `hocrgen export-public-beta` now packages a local mixed `HeOCR` public beta handoff tree with `manifests/public_beta_readiness_report.json`, explicit `pass`/`blocked` gates, release-level checksum and archive manifests, a portable version-rooted archive, beta-specific docs, and digest verification from the handoff tree; the current `F5` output remains blocked by the larger hocrsyngen synthetic target-scale gate and benchmark-reference readiness limits
  - `F5c`: `hocrgen export-public-beta` now also writes `manifests/public_beta_blocker_closure_plan.json`, categorizing blocked gates as `repo_owned_immediately_actionable` or `external_input_dependent`, sequencing privacy/review, benchmark-reference, and takedown closure work, and preserving the hard `2 / 80` synthetic target-scale blocker
  - `F5d`: `hocrgen export-public-beta` now also writes `manifests/public_beta_repo_owned_blocker_report.json`, which keeps repo-owned closure evidence machine-readable: unresolved review/privacy item ids and reasons, benchmark-reference draft/unavailable/adjudication status by item, and the latest private-reporting repository-settings check recorded in `src/hocrgen/config/public_beta.yaml`
  - `F6a`: the roadmap now names the post-F5 closure sequence as `F6`, with `F6b` through `F6g` queued so agents do not treat the current blocked public beta export as either finished or implementation-ready without real evidence
  - `F6b`: `src/hocrgen/config/public_beta.yaml` now records GitHub private vulnerability reporting for `HeOCR/hocrgen` as enabled and verified by the authenticated GitHub API check, and `export-public-beta` now drops `takedown_removal` from blocked gate and blocker-plan output only when that verified config evidence is present
  - `F6c`: `export-public-beta` now records that current benchmark-reference evidence is still partial rather than ready: one selected benchmark item is reviewed/adjudicated, one remains draft/in review, and one has no reference, so `benchmark_references` stays blocked until all selected benchmark references have real reviewed/adjudicated evidence and coherent versioning
  - `F6d`: `export-public-beta` now records that current privacy/review evidence is still unresolved rather than ready: the current governed candidate pool has review-required item evidence with default-unresolved review decisions and no repo-tracked review/config/source-status closure evidence, so `privacy_review` stays blocked until real closure evidence exists
  - `F6e`: `export-public-beta` now records that current source-depth/composition evidence is still insufficient rather than ready: the public-profile payload has only `1` NLI item, `1` Pinkas item, `0` BiblIA items, and `2` synthetic items, while F1c/operator-only/source-depth-only inventory is explicitly not counted as public beta readiness evidence
  - `F6f1`: the hocrsyngen S6 handoff review now defines a planning-only hocrgen adapter preflight before integration: the future operator-only dry-run may call installed hocrsyngen CLI JSON surfaces, validate public manifests/reports/assets/catalog joins, recompute hashes, and report missing hocrgen-owned release/import metadata without changing default release paths or asking hocrsyngen to mutate `generation_manifest.v1`
  - `F6f2a`: the hocrgen-side preflight reader now consumes a hocrsyngen evidence-run root without importing hocrsyngen internals or invoking release paths, validates `candidate_evidence_run_report.v1`, `generation_report.v1`, `validation_report.v1`, `template_catalog.v2`, public `generation_manifest.v1`, checksums, assets, and catalog joins, and emits an operator-only hocrgen diagnostic report with `release_eligible: false`
  - `F6f2`: the hocrgen-side preflight now also computes and optionally writes a `hocrgen_hocrsyngen_import_metadata_packet.v1` sidecar that covers the missing manifest-level provider metadata and per-sample rendering/Hebrew coverage metadata without mutating hocrsyngen `generation_manifest.v1`, importing hocrsyngen internals, or wiring the batch into release/export paths
  - `E4a`: multi-release governance is now documented around release version semantics, removal/takedown handling, additive schema migration policy, source deprecation rules, benchmark stability guarantees, and release compatibility statements without changing current public or alpha inclusion behavior
  - `E3a`: annotation pilot selection is now config-driven for a tiny explicit subset, validates release-ready and benchmark membership before emitting pilot manifests, uses release-relative planned target paths, and keeps current public/alpha outputs free of mandatory transcription or layout-label requirements
  - `E2b`: vetted NLI seeds can now be selected from the exploratory catalog, runnable seed manifest, or both; local fixture-backed seeds are reused without network access; live acquisitions still write reusable fixtures/assets plus a machine-readable report with promoted, skipped, and failed seeds
  - `E2a`: benchmark examples can now be loaded from current benchmark/export artifacts, JSONL text predictions can be evaluated against explicit references, and reports include coverage, character error rate, exact-match rate, per-item edit distances, and lightweight leaderboard-ready conventions
  - `E1a`: contribution policy now defines source proposal workflow, review policy for external changes, synthetic asset contribution rules, dataset issue taxonomy, source-adapter contribution guidance, and release governance notes without adding broad ingestion behavior
  - `D5a`: item manifests now reserve optional transcription and layout-label references, build/alpha outputs include annotation manifests, and current public releases still do not require transcriptions
  - `D4b`: synthetic generation can now be filtered by D4a template, recipe, and degradation metadata, and build/alpha outputs include synthetic composition reporting
  - `D4a`: richer synthetic rendering is implemented without new external assets by adding recipe metadata, form-like printed pages, marginalia-style handwritten pages, stronger deterministic scan degradation, and default public output coverage for both recipes
  - `D3a`: benchmark subset work is implemented as a small explicitly approved `benchmark_v1` slice with benchmark manifests, audit output, a stability policy, usage guidance, and packaged approval config for non-editable installs
  - `D2a`: source-health check paths now prefer portable operator references by reporting packaged resources as `package://...`, config-root inputs relative to the active config root, and only truly external local inputs as absolute paths
- Planning workflow:
  - every roadmap-tracked PR should update `.agent-plan.md`, `README.md`, and any affected human-facing planning docs with current-ref language
  - roadmap notation is location-based: if `D4a` appears in docs on a feature branch it means implemented on that ref; after merge, the same wording means implemented on `main`
  - before starting a roadmap item, verify whether it is already merged on `main` and reflected in recent PR history
  - if merged code and planning docs disagree, reconcile the planning docs before treating the roadmap item as still open
  - roadmap-tracked and planned-milestone PRs should use `<notation>: <sentence-case summary>` titles and a top-level `## Planning notation` section in the PR body
  - feature/PR work is not complete until a non-draft GitHub PR with a detailed description, appropriate labels, and a milestone assignment is open

## Active Task Breakdown
- [x] `B2b` Replace scaffold-grade Pinkas/BiblIA fixture assets with real packaged/open historical sample pages
- [x] `B3b` Upgrade synthetic generation to use governed fonts, curated Hebrew text, plausible layouts, raster output, and scan/degradation realism
- [x] Add direct `HeOCR` checkout targeting and handoff-document generation to `export-alpha`
- [x] Re-run `export-alpha` from current `main` and inspect the resulting `alpha-v0` contents for final alpha freeze readiness
- [x] `B5b1` Sanitize alpha export manifests and review artifacts so release outputs stay portable and do not expose local filesystem paths
- [x] `B5b2` Refresh the public real-scan exemplars with higher-resolution NLI exports and text-bearing historical sample pages
- [x] `B5b3` Fix synthetic Hebrew ordering, move alpha synthetic inclusion to a `2x real items` cap, and land only low-risk realism improvements needed for alpha credibility
- [x] `B5b4` Re-run `export-alpha`, perform the final manual inspection, hand the frozen tree into the separate `HeOCR` repository, and open the coordinated ready-for-review PRs
- [x] `C5b` Merge structured review decisions back into the pipeline, add repo-tracked allow/block overrides, and harden the operational review loop
- [x] `C6a` Add release diffs, baseline selection, removal reasons, and changelog automation to `export-alpha`
- [x] Update `.agent-plan.md`, `README.md`, and affected planning docs in the same branch whenever a roadmap-tracked PR is opened
- [x] Reconcile the planning docs after `C6a` merged so merged `main` remains the canonical status view
- [x] `D1a` Add resumable dry-run run directories plus `summarize-run` reporting for GitHub Actions maintenance jobs
- [x] `D1a` Add reusable `hocrgen-dry-run` and weekly `expansion-maintenance` workflows for scheduled dry-run/reporting automation
- [x] `D2a` Add typed source operations config, fixture-backed source health checks, and source freeze/degrade reporting
- [x] `D2a` Add planning consistency coverage so current-state docs cannot silently drift after planned work lands
- [x] `D2a` Make source-health check paths portable in operator artifacts where package or config-root references are available
- [x] `D3a` Add explicit benchmark v1 approvals, benchmark manifests, selection audit, stability policy, and benchmark card output
- [x] `D3a` Package benchmark approval config under `hocrgen.data` so non-editable installs do not require checkout-root `benchmark_data/`
- [x] `D4a` Add recipe-backed synthetic visual realism, richer handwritten/printed page treatments, and deterministic degradation metadata
- [x] `D4b` Add synthetic diversity controls, filter/reporting surfaces, and composition reporting
- [x] `D5a` Add optional transcription-ready architecture foundations with typed annotation manifests
- [x] `E1a` Add community contribution model and contribution safety rails for source proposals, source adapters, synthetic assets, dataset issue taxonomy, and release governance
- [x] `E2a` Add lightweight benchmark example loading, text metric helpers, evaluation reports, and leaderboard-ready conventions
- [x] `E2b` Add roadmap-backed live/cached batch acquisition for vetted NLI seed URLs so release-size real-sample growth does not require one-by-one manual promotion
- [x] `E3a` Add carefully bounded annotation subset pilots for selected release/benchmark items without making annotations mandatory
- [x] `E4a` Add multi-release governance maturity controls for versioning, removals, schema migration, source deprecation, benchmark stability, and release compatibility
- [x] `F1a` Define the operator-only beta-scale acquisition trial plan before implementation
- [x] Initialize a repo-native Splendor workspace and ingest high-signal hocrgen docs, schemas, workflows, config, source-ops, release/export, benchmark, annotation, review, and fetcher sources
- [x] `F4a` Record the four-repository synthetic spinout architecture before synthetic-provider implementation
- [x] `F1b` Implement beta-trial source-depth feasibility and operator reporting
- [x] `F1b2` Define Pinkas/BiblIA source-depth expansion feasibility before F1c execution
- [x] `F1b3` Expand source-depth counts before F1c execution while keeping the trial operator-only
- [x] `F1b4` Expand NLI runnable/cached source depth before F1c execution using real source-cached fixtures, not generated stand-ins
- [x] `F1c` Execute bounded beta-trial acquisition artifacts only after feasibility gates pass
- [x] `F1d` Add near-duplicate, source-group, and split-leakage hardening before scale beyond the trial
- [x] `F1e` Resolve benchmark/holdout leakage gate before public beta/export readiness claims
- [x] `F2a` Define benchmark ground-truth guidelines for transcription, layout labels, and reference manifests
- [x] `F2b` Implement benchmark-reference ingestion, adjudication artifacts, and benchmark versioning gates
- [x] `F3a` Define rights-clean modern handwritten Hebrew acquisition policy
- [x] `F3b` Implement bounded modern handwritten acquisition workflow and typed intake manifests
- [x] `F4b` Define and ingest the external hocrsyngen provider manifest contract with fixture validation
- [x] `F4c` Add deeper Hebrew rendering/provider metadata gates for hocrsyngen batches
- [x] `F4d` Add synthetic-only export handoff for `HeOCRsynth`
- [x] `F4e` Extract shared release export packaging primitives
- [x] `F4f` Document the wider HeOCR ecosystem upstream chain (`public-domain-hand-written-hebrew-scans` → `hletterscriptgen` → `hletterscript` → `hocrsyngen` → `hocrgen` → `HeOCR` / `HeOCRsynth`) without changing runtime behavior or release gates
- [x] `F5a` Define public beta readiness gates after F1-F4 while keeping operator trial success separate from public publishability
- [x] `F5b` Implement public beta publication packaging and handoff workflow against the F5a readiness gates
- [x] `F5c` Close public beta readiness blocker sequencing and repo-owned handoff gaps without relaxing any readiness gate
- [x] `F5d` Close repo-owned public beta readiness gates before external scale inputs by emitting precise closure evidence without relaxing any gate
- [x] `F6a` Define the post-F5 public beta closure roadmap before further implementation
- [x] `F6b` Close takedown/private reporting readiness with verified GitHub private vulnerability reporting evidence while keeping unrelated blockers blocked
- [x] `F6c` Close benchmark-reference readiness only with real reviewed/adjudicated reference evidence; otherwise preserve the limitation as blocked reporting
- [x] `F6d` Close privacy/review blockers only through review/config/source-status changes; otherwise preserve the limitation as blocked reporting
- [x] `F6e` Close source-depth/composition readiness only with real public-profile candidate evidence; otherwise preserve the limitation as blocked reporting
- [x] `F6f1` Plan the hocrsyngen installed-CLI adapter preflight and record the public manifest versus hocrgen release/import metadata gap
- [x] `F6f2a` Implement the hocrsyngen evidence-root preflight/import-packet reader without making batches release-eligible
- [x] `F6f2` Define the hocrgen-owned release/import metadata sidecar for validated hocrsyngen evidence roots without making batches release-eligible
- [ ] `F6g` Re-run public beta readiness after privacy/review closure, source-depth, and synthetic-scale evidence exists
- [ ] Keep later source-ops follow-ups queued in the long-term roadmap
- [x] Reconcile issue #29 as the source-health path reporting source-ops follow-up on the current ref

## Context Pointers
- Roadmap: [docs/HeOCR_hocrgen_long_term_roadmap.md](./docs/HeOCR_hocrgen_long_term_roadmap.md)
- Pre-alpha execution plan: [docs/pre_alpha_freeze_plan.md](./docs/pre_alpha_freeze_plan.md)
- Design/spec: [docs/hocrgen_design_and_spec.md](./docs/hocrgen_design_and_spec.md)
- Current repo capabilities and commands: [README.md](./README.md)
- Normalization and QA details: [docs/hocrgen_normalization_and_qa.md](./docs/hocrgen_normalization_and_qa.md)
