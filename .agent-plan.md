# Agent Plan

## Current System State
- Active branch: `codex/pre-alpha-freeze-plan`
- Last action taken: re-ran `export-alpha` from `main`, inspected the generated `alpha-v0` tree, and translated the findings into a pre-alpha freeze execution plan
- Current blockers:
  - `B5b1`: exported public artifacts still leak absolute local paths into release manifests and review audit files
  - `B5b2`: current alpha exemplars are not yet defensible because the NLI sample is too small and the Pinkas sample is a binding/cover image rather than a text-bearing page
  - `B5b3`: synthetic alpha samples still need Hebrew ordering fixed and the alpha sampling cap should move to a `2x real items` policy
  - `B5b4`: the validated release tree still needs a final freeze pass and handoff PR into the separate `HeOCR` repository
- Post-alpha follow-ups:
  - `C5b`: review-decision merge is still future work, so unresolved review-required items remain outside the public alpha payload
  - `D4a` / `D4b`: handwritten-like synthetic rendering, stronger post-processing, and broader synthetic diversity are still important, but they are explicitly post-alpha work unless a low-risk improvement falls out of the alpha-blocker fixes
- Planning workflow:
  - every roadmap-tracked PR should update `.agent-plan.md`, `README.md`, and any affected human-facing planning docs in the same branch

## Active Task Breakdown
- [x] `B2b` Replace scaffold-grade Pinkas/BiblIA fixture assets with real packaged/open historical sample pages
- [x] `B3b` Upgrade synthetic generation to use governed fonts, curated Hebrew text, plausible layouts, raster output, and scan/degradation realism
- [x] Add direct `HeOCR` checkout targeting and handoff-document generation to `export-alpha`
- [x] Re-run `export-alpha` from current `main` and inspect the resulting `alpha-v0` contents for final alpha freeze readiness
- [ ] `B5b1` Sanitize alpha export manifests and review artifacts so release outputs stay portable and do not expose local filesystem paths
- [ ] `B5b2` Refresh the public real-scan exemplars with higher-resolution NLI exports and text-bearing historical sample pages
- [ ] `B5b3` Fix synthetic Hebrew ordering, move alpha synthetic inclusion to a `2x real items` cap, and land only low-risk realism improvements needed for alpha credibility
- [ ] `B5b4` Re-run `export-alpha`, perform the final manual inspection, and hand the frozen tree into the separate `HeOCR` repository
- [ ] Update `.agent-plan.md`, `README.md`, and affected planning docs in the same branch whenever a roadmap-tracked PR is opened
- [ ] Keep handwriting-like synthetic generation, heavier degradation/post-processing, synthetic diversity/reporting work, and `C5b` review-decision merge queued as post-alpha follow-ups in the long-term roadmap
- [ ] Plan the post-alpha follow-up for `C5b` review-decision merge after the first HeOCR alpha handoff is stable

## Context Pointers
- Roadmap: [docs/HeOCR_hocrgen_long_term_roadmap.md](./docs/HeOCR_hocrgen_long_term_roadmap.md)
- Pre-alpha execution plan: [docs/pre_alpha_freeze_plan.md](./docs/pre_alpha_freeze_plan.md)
- Design/spec: [docs/hocrgen_design_and_spec.md](./docs/hocrgen_design_and_spec.md)
- Current repo capabilities and commands: [README.md](./README.md)
- Normalization and QA details: [docs/hocrgen_normalization_and_qa.md](./docs/hocrgen_normalization_and_qa.md)
